{{ $selectors := list }}
{{ $selectors = mustAppend $selectors (dict "key" "app" "value" (include "common.names.name" .) ) }}
{{ $selectors = mustAppend $selectors (dict "key" "release" "value" .Release.Name ) }}
{{ $ports := list }}
{{ $ports = mustAppend $ports (dict "name" "external_control" "port" .Values.external_control_port "nodePort" .Values.external_control_port "targetPort" .Values.external_control_port) }}
{{ $ports = mustAppend $ports (dict "name" "mixed" "port" .Values.mixed_port "nodePort" .Values.mixed_port "targetPort" .Values.mixed_port) }}
{{ $params := . }}
{{ $_ := set $params "commonService" (dict "type" "NodePort" "ports" $ports ) }}
{{ $_1 := set .Values "extraSelectorLabels" $selectors }}
{{ include "common.classes.service" $params }}
